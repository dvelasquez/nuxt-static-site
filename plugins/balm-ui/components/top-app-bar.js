import Vue from 'vue'
import 'balm-ui/components/top-app-bar/top-app-bar.css'
import UiTopAppBar from 'balm-ui-source/components/top-app-bar'

// Optional. Overwrite `<ui-top-app-bar>` props with default value.
Vue.use(UiTopAppBar, {
  // some props
})
